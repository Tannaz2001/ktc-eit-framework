data_plugin: KTCDataPlugin
mesh_path: Codes_Matlab/Mesh_sparse.mat

levels:
  - 1
  - 2
  - 3
  - 4
  - 5
  - 6
  - 7

samples:
  - A
  - B
  - C

methods:
  - MockMethodPlugin
  - BackProjection
  - GaussNewton

# dataset_root is overridden at runtime by KTC_DATASET_ROOT env var if set.
# Default points to the EvaluationData folder inside the repo.
dataset_root: EvaluationData
output_dir: outputs/
