# Manifest of direct dependencies. For a reproducible install use the
# pinned snapshot instead:  pip install -r requirements.lock.txt
# (regenerate after dependency changes with:  pip freeze > requirements.lock.txt)


# Core dependencies (original)
numpy
pytest
scipy
pyeit

# Streamlit dashboard dependencies
streamlit>=1.28.0
plotly>=5.17.0
kaleido==0.2.1
pandas>=2.0.0
pyarrow>=14.0,<18.0
reportlab>=4.0.0

# Visualization dependencies (from README)
matplotlib
seaborn
scikit-image
rich
pyyaml
h5py

# CompetitionCNN subprocess dependencies
# These run on system Python 3.12, NOT inside this venv.
# On a fresh PC run:
#   py -3.12 -m pip install numpy scipy matplotlib scikit-image tensorflow opencv-python
